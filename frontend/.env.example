# Optional. Leave VITE_API_URL empty in dev: the Vite proxy forwards /api to the backend.
VITE_API_URL=
VITE_BANK_NAME=Bank X
# Where the dev proxy sends /api (default http://127.0.0.1:8000)
VITE_DEV_API_TARGET=
